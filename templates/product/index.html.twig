{% extends 'base.html.twig' %}

{% block title %}Les produits - MM{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-3">
            <h3>Filtres</h3>
            {{ form(form) }}
        </div>
    <h1>Nos produits</h1>
    <div class="row">
        {% for product in products %}
            <div class="col-md-4">
                {% include 'product/single_product.html.twig' %}
            </div>
        {% endfor %}
    </div>
{% endblock %}
