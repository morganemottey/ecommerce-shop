{% extends 'base.html.twig' %}

{% block title %}Mes adresses - MM{% endblock %}

{% block content %}
    <a href="{{ path('add_account_adress') }}" class="btn btn-dark btn-sm float-right" >Ajouter une adresse</a>
    <h1> Mes adresses </h1>
    C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.
    <a href="{{ path('account') }}" >Retour</a>
    <hr>
    {% if app.user.adresses|length == 0 %}
        <p class="text-center">
            Vous n'avez pas encore ajoutés d'adresses dans votre compte client. Pour en ajouter veuillez 
            <a href="{{ path('add_account_adress') }}" >cliquer-ici</a>
        </p>
    {% else %}
    <div class="row">
       {% for adress in app.user.adresses %}
       <div class="col-md-4">
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{ adress.name }}</h5>
                <p class="card-text">{{ adress.adress}} <br>
                {{adress.codepostal}} - {{ adress.city}}
                </p>
                <a href="{{ path('edit_account_adress', { 'id' : adress.id }) }}">Modifier</a> | <a href="{{ path('delete_account_adress', { 'id' : adress.id }) }}" class="">Supprimer</a>
            </div>
        </div>
       </div>
       {% endfor %}
    </div>
    {% endif %}
{% endblock %}
